@Library('license-scan-shared') _   
pipeline {
    agent any

    stages {
        stage('License Scanning') {
            steps {
                script {
                    licenseScanningGeneric(
                        reportDir    : 'reports',
                        slackChannel : '#ci-operation-notifications'
                    )
                }
            }
        }
    }
}